.title
  Title
  %br/
  = f.text_field :title

.slug
  Slug
  %br/
  = f.text_field :slug

.announcement
  Announcement
  %br/
  = f.text_area :announcement

.report
  Report
  %br/
  = f.text_area :report

.type
  Type of event:
  %br/
  Meetup #{f.radio_button :whatisit, 'meetup'}
  %br/
  Hackathon #{f.radio_button :whatisit, 'hackathon'}

.scheduled_at
  = datetime_select :event, :scheduled_at

%p.active
  Event is active?: 
  = f.check_box :is_active
