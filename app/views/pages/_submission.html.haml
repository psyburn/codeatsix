%div.subtitle Prijave
-if event
  = form_tag apply_path, :id => 'apply-form' do |f|
    = hidden_field_tag :event_id, event.id
    #name-div.text_field
      = text_field_tag :name, "", :placeholder => "ime/prezime"
    #email-div.text_field
      = email_field_tag :email, "", :placeholder => "email"
    =submit_tag 'Prijavi se', :class => 'button', :id => 'apply'
  .clearboth
  #messages
- else
  %p Trenutno nema eventa na koji se mozete prijaviti.
